<!DOCTYPE html>
<html>
  <head>
    <title>Hw71</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <style>
      body {
        padding: 0;
        margin: 0;
        padding-top: 90px;
      }
      .header {
        width: 100%;
        height: 40px;
        border: 1px solid red;
        position: fixed;
        top: 0;
        left: 0;
      }
      .center {
        margin-left: auto;
        margin-right: auto;
      }
      .img1 {
        width: 20px;
        height: 20px;
        margin: 5px;
      }
    </style>
  </head>

  <header id="header">
    <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
      <div class="collapse navbar-collapse">
        <form class="form-inline my-2 my-lg-0" action="/search">
          <input class="form-control mr-sm-2" name="q" type="text" placeholder="Search" aria-lable="Search">
          <button class="btn btn-outline-success my-2 sm-0" type="submit">Search</button>
        </form>
      </div>

      <div id="user_header">
        <% if current_author %>
          Logged in as <%= current_author.email %>.
          <%= link_to "Log Out", session_path("current"), method: "delete" %>
        <% else %>
          <%= link_to "Sign Up", new_author_path %> or
          <%= link_to "Log In", new_session_path %>
        <% end %>
      </div>
    </nav>
  </header>

  <body>
    <%= yield %>
  </body>
</html>
