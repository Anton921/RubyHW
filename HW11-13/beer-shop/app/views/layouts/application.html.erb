<!DOCTYPE html>
<html>
  <head>
    <title>BeerShop</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <style>
      .header {
        width: 100%;
        height: 40px;
        border: 1px solid red;
        position: fixed;
        top: 0;
        left: 0;
      }
      
    </style>
  </head>

  <body>
    <header id="header">
      <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <div class="collapse navbar-collapse">
          <%= link_to 'Home', products_path, class: 'btn btn-danger' %>
          <form class="d-flex" action="/search">
            <input class="form-control mr-sm-2" name="q" type="text" placeholder="Search" aria-lable="Search" data: {autocomplete: products_autocomplete_product_title_path}>
            <button class="btn btn-outline-success my-2 sm-0" type="submit">Search</button>
          </form>
        </div>
      </nav>
    </header>

    <main>
      <div class="row ">
        <div class="col-sm-3 col-12">
          <nav class="nav nav-pills flex-column bg-dark">
            <%= render "categories/categories", collection: @categories %>
          </nav>
        </div>
        <div class="col-sm-9 col-12">
          <%= yield %>
        </div>
      </div>
    </main>
  </body>
</html>
